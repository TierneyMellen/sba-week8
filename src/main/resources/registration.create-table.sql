CREATE IF NOT EXISTS TABLE registration(
    id          INT NOT NULL,
    email        VARCHAR(50) NOT NULL--,
    --FOREIGN KEY (id) REFERENCES courses(id),
    --FOREIGN KEY (email) REFERENCES students(email)
);